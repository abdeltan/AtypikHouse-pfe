<nav id="header" class="navbar navbar-expand-md navbar-white bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="{{ asset('/images/atypikHouse.svg') }}" alt="Logo" height="50">
        </a>
        <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false"
            aria-label="Toggle navigation">
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 navbar-nav-scroll">
                <li class="nav-item">
                    <a class="nav-link text-dark active" aria-current="page" href="{{ path('home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path('properties') }}">Nos hébergements</a>
                </li>
                {% if app.user != null %}
                {% if "ROLE_SUPERADMIN" not in app.user.roles  %}
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path('host') }}">Devenir hébergeur</a>
                </li>
                {% endif %}
                {% endif %}
            </ul>
            <ul class="ms-auto navbar-nav">
                {% if app.user == null %}
                <li class="nav-item">
                    <a class="nav-link text-dark" role="button" href="{{ path('app_register') }}">Créer
                        un compte</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" role="button" href="{{ path('login') }}">Se connecter</a>
                </li>
                {% else %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        {{ app.user.firstName }}&nbsp;{{ app.user.lastName }}
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('my-account') }}">Mon compte</a></li>
                        <li><a class="dropdown-item" href="{{ path('reservations') }}">Mes réservations</a></li>
                        {% if "ROLE_ADMIN" in app.user.roles %}
                        <li><a class="dropdown-item" href="{{ path('dashboard') }}">Espace hébergeur</a></li>
                        {% elseif  "ROLE_SUPERADMIN" in app.user.roles  %}
                        <li><a class="dropdown-item" href="{{ path('dashboard') }}">Espace Admin</a></li>
                        {% endif %}
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="{{ path('logout') }}">Déconnexion</a></li>
                    </ul>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>

</nav>